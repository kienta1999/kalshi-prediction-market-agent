# Secrets — Kalshi API key id + RSA private key. NEVER commit.
kalshi-prediction-market-agent.txt
*.pem
*.key

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/

# Logs (runtime trade journal — keep lessons.md tracked, ignore raw jsonl)
logs/*.jsonl

# paper-invest runtime state: concurrency lock + once-per-day markers + transcripts
logs/.paper-invest.lock
logs/.*-paper.done
logs/*-paper.run.log

# OS
.DS_Store
